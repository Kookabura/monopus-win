# monOpus service setup

Run `.\action_scripts\setup\Install-Monitor-Host.ps1` from an elevated Windows PowerShell window in the repository root. See the root README for parameters, service name, configuration path, and installation log.

`Update-Monitoring.ps1` is an older remote deployment tool for scheduled tasks. It is not part of the service installer.
